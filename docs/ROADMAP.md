# Venice Forge Roadmap

This is the canonical ledger for current unfinished work only. Closed execution history and validation evidence belong in `docs/summary_of_work.md`; retained scan reports are audit input, not current status authority.

## Current Work

The locally actionable findings `VF-SCAN-20260722-001..014` from `docs/audits/Records/Venice_Forge_Extensive_Scan_2026-07-22.md` were corrected and regression-tested on 2026-07-22. Their immutable snapshot status fields remain historical; the dated remediation addendum and `docs/summary_of_work.md` carry the live disposition. The Document Agent file in `docs/audits/Records/Function_calling_todo.md` is an implementation and acceptance specification, not a checkbox-derived status ledger; unfinished product scope is represented only by `VF-DOCUMENT-AGENT-001` below.

The 2026-07-23 Image Inspector local regression tranche is closed: image ingestion is main-owned and bounded, model output is schema-validated, provider-authored failures are preserved, diagnostics are idempotently redacted, and the UI describes query-only results as text-based potential sources rather than reverse-image matches. Paid live-provider image acceptance remains external evidence under `VF-VERIFY-005`.

`VF-CHARACTER-CREATOR-HARDENING-001 — Character Creator Integration, Visible Process & Hardening (Reopened for P0/P1 Master Work Order 2026-07-25).` Active remediation against `docs/audits/TODO/VENICE_FORGE_MASTER_COMPLETION_WORK_ORDER_2026-07-25.md` resolving import candidate handle consumption, autosave durability & flush failure handling, IPC channel consolidation, export hardening, and honest AI design process representation:

- **Closed (P0-01):** Idempotent atomic creation under concurrency. `CharacterCreatorImportService.approveAndCreateCharacter` is fronted by a per-draft FIFO JS-layer mutex (`approveLockByDraft` / `withDraftApprovalLock` in `src/services/characterCreatorImportService.ts`) that serialises every approval of the same draftId so concurrent callers see the canonical existing character instead of racing. As belt-and-suspenders inside the serialised body, `CharacterDraftService.tryMarkCreated(id, patch, expectedRevision)` performs a last-writer-wins verify on the post-save read; new characters generate IDs with `crypto.randomUUID()`. Regression: `tests/character-creator/independentAuditProbe.test.ts` (1 file / 3 tests, all PASS).
- **Closed (P0-02):** Guaranteed state-level compensating rollback. `rollbackCreatedCharacter(savedCharacterId, previousCharacter, isUpdate)` always force-clears `useCharacterCardStore.getState().cards` for `savedCharacterId` when `useCharacterCardStore.getState().remove` returns `false` or rejects — first via a direct `useCharacterCardStore.setState({ cards: filteredCards })`, and finally an outer `try/catch` force-filter that runs even if both earlier layers reject.
- **Closed (P1-03):** Cross-runtime backup fixture recognises the new store. `tests/backup/cross-runtime-backup.test.ts:webProfileBoundPayload` declares `character_creator_drafts: []` alongside the existing store fixtures so the Web round-trip `toEqual(webProfileBoundPayload)` matches the actual decrypted envelope.
- **Closed (P1-04):** Canonical Launch Intent Flow & Entry Point Integration. `useCharacterCreatorLaunchStore` and helper `openCharacterCreator` route all AI-assisted character creation or editing entry points (RP Studio CharacterLibrary, RP Studio CharacterEditor, Welcome Screen) into Character Creator. Transient launch intents are consumed exactly once (`consume()`).
- **Closed (P1-05):** Immutable Model Lock (`zai-org-glm-5-2`). Model lock `CHARACTER_CREATOR_MODEL_ID = "zai-org-glm-5-2"` is enforced across all request builders, AI services, draft sanitizers, and UI components. Any override attempt throws `CharacterCreatorModelOverrideError`. Model unavailability produces typed `MODEL_UNAVAILABLE` error states while preserving draft data.
- **Closed (P1-06):** Real Event-Driven Visible AI Process & Animated Mascot Remediation. Progress is driven strictly by `CharacterCreatorProcessEvent` updates (`queued`, `concept-analysis`, `card-draft`, `consistency-review`, `schema-validation`, `repair`, `complete`) displayed in `CharacterCreatorProcessPanel`. Replaced `UserRoundPen` icon with animated Mio mascot GIF (`mio-xc3-nerdprofeta-jumping.gif`) and reduced-motion static PNG fallback (`mio-xc3-nerdprofeta-jumping-static.png`).
- **Closed (P1-07):** Local Picker Modal, Debounced Autosave & Contract Verification. Implemented `CharacterCreatorLocalPickerModal` for selecting local cards to edit. Implemented 600ms debounced autosave with explicit flush (`flushPendingSave`) before validation, approval, and export. Verification: `npm run lint:eslint` (0 warnings), `npm run typecheck` (0 errors), `npm run verify:contracts` (103/103 passed), `npm run build` (PASS), `npx vitest run tests/character-creator/` (18/18 passed).

`VF-IMAGE-SEARCH-001 — Direct source-image web matching (blocked on provider/product decision).` The 2026-07-24 follow-up added confirmation-gated inspection deletion and live catalog pricing while preserving vision-only model selection. The requested raw-image Google/Brave lookup cannot use the existing Venice `/augment/search` integration: its provider contract accepts only a 1–400 character text query, and Brave's official Image Search API likewise requires `q` rather than source-image bytes. Completion requires an explicitly selected supported reverse-image provider (for example, a Google Cloud Vision Web Detection integration), new main-process credential custody and endpoint allowlisting, transmission disclosure/consent, response normalization, cost presentation, and provider-backed tests. Do not revive the current AI-generated-query path as if it were direct image matching.

`VF-CHAT-FOLDERS-MEDIA-DOCUMENTS-001 — Chat folders, agent media, documents, and video work order (closed by 9-phase implementation + vo-validate).` Closed by the 2026-07-19 nine-phase work order (`docs/work-orders/VENICE_FORGE_CHAT_FOLDERS_MEDIA_DOCUMENTS_2026-07-19.md`): Phase 0 baseline + cleanup of 25 transient `patch_*.cjs` files; Phase 1 standard-vs-character folder separation with `selectStandardFolders`/`selectCharacterFolders` selectors + `loadFolders(kind?)` merge-select; Phase 2.x renderer-side closure of 7 missing folder operations + the lib sodium Argon2id13 + XChaCha20-Poly1305-IETF folder-backup encryption rewrite (270 → 433 lines) with `_sodiumReadyPromise` lazy loading, v2 wire format with `publicHeader` preview, `BackupStructureError` class, `folderLockAfterImport()` lock-state downgrade; Phase 3 audit-confirmed trusted tool runtime and date/time layer; Phase 4 prompt-limit thresholds migrated from binary KiB to Unicode code points (8,000 warning / 12,000 hard maximum + 16,000 large-context opt-in); Phase 5.x removed unimplemented `media.generateVideo`/`generateAudio` tools and redacted all 3 raw `error.message` sites in `agent-tool-executor.ts` with `sanitizeErrorText()`; Phase 6 widened `ChatMediaReference` to the canonical list schema (id / mediaId / mediaType / operation / displayUrl / optional thumbnailUrl / altText / modelId / createdAt / deletedFromChatAt) and added `recordGeneratedMediaForMessage`/`removeMediaReferenceFromMessage`/`restoreMediaReferenceOnMessage` save-sequence helpers with transient `orphanedGeneratedMediaRefs` + `tombstonedMediaRefs` registries; vo-shared parity collapse created `src/shared/chatMediaReferenceContracts.{ts,test.ts}` (87/87 PASS across 4 parity files); Phase 7 fixed `src/agent/registry/tool-registry.test.ts` typecheck error (cast slot via `ProviderToolName` from `tool-name-map`); Phase 8 audit-confirmed video-byte-range + CORS contract; Phase 9 added the `diagnosticsIncludePrompts` opt-in (off by default), per-excerpt 80-char cap, 5-element cap, djb2 hash + sanitized excerpt, and audit-correlation metadata in settings-store v10 → v11. Final vo-validate matrix green: lint (0), typecheck (0), test:ci (228/228 across 21 files), 5 base verifiers (image-policy / workspace-contracts 196 / safety-guard / markdown-links 134 / contracts 103 / repo-handoff-hygiene), build OK. Final implementation report: `docs/reports/historical/VENICE_FORGE_CHAT_FOLDERS_MEDIA_DOCUMENTS_REPORT_2026-07-19.md` (22 sections). Open follow-up is OFF this ledger (see §"Active Audit Reconciliation"): only documentation pointers (DOCS_INDEX + ROADMAP) and a manual on-device QA click-through (deferred; engine-room coverage already exercises every contract).

> **Reopened for P0 remediation — 2026-07-20 session.** The 2026-07-20 audit (`docs/audits/Records/Venice_Forge-audit-evidence-20260720-*/`) re-examined the closed work-order artifacts and identified five P0 defects that survived the 9-phase closure. Each is now remediated with regression coverage; close the row once `docs/summary_of_work.md` records the 2026-07-20 validation matrix.
>
> - **P0-01 — Chat folder backup import silently dropped conversations.** `importBackup` in `electron/services/chatFolderBackupService.ts` previously declared success without invoking `saveConversation`/`saveImportTarget` per conversation. Fixed: per-conversation `saveConversation` with rollback on partial failure and merge-mode remap via `crypto.randomUUID()`. Regression: 8 new assertions in `electron/services/chatFolderBackupService.test.ts`.
> - **P0-02 — Default-profile chat folder wrote to the wrong directory.** `saveChatFolder(folder)` resolved the path against the legacy non-default fallback rather than `userData/chat-history/default/`. Fixed: explicit `saveChatFolder(folder, profileId)` signature that resolves against the active profile directory; non-default profiles continue to use validated subdirectories. Regression: 5 new assertions in `electron/services/chatFolderBackupService.test.ts`.
> - **P0-03 — Agent `media.generateImage` returned a non-canonical `{mediaId, mimeType}` metadata stub.** The chat-agent runner emitted a stub object instead of a `ChatMediaReference[]`, so the chat-store Media Studio upsert path silently skipped the entry and rendered a broken link. Fixed: new helper `extractCanonicalChatMediaReferences(toolResult)` projects the executor's `data.chatRef` into the validator-checked contract (`isChatMediaReferenceArrayContract`) and attaches it to the tool message; legacy stub emission is dead. Regression: 4 assertions in `electron/agent/runtime/chat-agent-runner.test.ts`.
> - **P0-04 — `executeMediaTool` bypassed the guarded Venice broker with a raw `fetch()`.** Calls to `https://api.venice.ai/api/v1/image/generate` skipped `performGuardedVeniceRequest`, so Local Family Safe Mode preflight, runtime `safe_mode`, response screening, retries, abort, idempotency, and inspector logging were all bypassed. Fixed: `executeMediaTool` now routes through `performGuardedVeniceRequest({endpoint:"/image/generate", method:"POST", body, profileId})`; other `media.*` tools fail closed with `CAPABILITY_DENIED`; response is validated, persisted via `persistGeneratedMedia`, and returned as canonical `data.chatRef`. Regression: 7 assertions in `electron/agent/runtime/agent-tool-executor.test.ts`.
> - **P0-05 — `composeTrustedRequest` discarded the tool runtime layer and only injected for `/chat/completions`.** The previous code did `void buildToolRuntimeLayer(tools)` (silently discarded the layer) and only prepended the runtime context on `/chat/completions`, leaving `/image/generate` and other endpoints without temporal grounding. Fixed: tool layer now attached via a "Toolchain Trust Ledger" section; injection runs for every POST endpoint (chat via `messages[0]`, non-chat via `body.prompt` prefix); `{{time && date}}` placeholder substitution runs against the system prompt; `composeAgentRuntime` now enforces an immutable priority floor (priority < 0 forbidden once trusted layer is registered) and dedupes identical tool-runtime layers by FNV-1a content hash. Regression: 7 new assertions in `electron/agent/runtime/trusted-agent-request.test.ts`.

`VF-MEDIA-PREVIEW-TRAFFIC-INSPECTOR-001 — Media Preview + Traffic Inspector remediation (Phases A–D + Phase C emitter wiring, closed).` The four-phase work order (2026-07-26, committed at `440fb2e88da37a17baf8208e7ec68d7d5f849928`, 27 files / +1418 / −156, `origin/main` push verified) covered the durable image preview path, the legacy `venice()` request helper, the cross-process traffic telemetry pipeline, and the Inspector pane UI semantics. Phase C (2026-07-27) closed the deferred `TOD-TELEMETRY-EMITTERS-001` follow-up by wiring the remaining five Phase C emitters so every outbound Venice / Jina / guard event flows through the Inspector surface: `electron/services/videoRetrieveService.ts` (`/video/queue` + `/video/retrieve`, completion summaries omits `download_url`), `electron/ipc/handlers/jinaHandlers.ts` (`runJinaRequest` inner with guard-block + body-screen + URL-validator paths; the `kind: "fail"` return now carries `body?: SafetyBlockBody` so the canonical block payload reaches callers and the Jina CSAM regression test re-passes), `electron/services/guardPipeline.ts` (`checkLocalFamilyGuard` emits request + completion with `guardOutcome`; `performGuardedVeniceRequest` response-screen block + `SafetyGuardBlockedError` rewrap emit completion with `blocked:REASON_CODE:CATEGORY`), `electron/agent/runtime/agent-tool-executor.ts` (image generate lifecycle, success → status 200, block → status 451, dispatcher throw → completion + rethrow), and `electron/agent/runtime/chat-agent-runner.ts` (one telemetry pair per chat-agent turn; streaming + tool-result round trips collapse to a single Inspector row reusing `eventId`). All emitter work is inside try/catch isolation so telemetry never breaks host operations. Regression coverage: `VERIFY-153` (`electron/ipc/handlers/jinaHandlers.test.ts`), `VERIFY-154` (`electron/services/guardPipeline.test.ts`), `VERIFY-155` (`electron/services/videoRetrieveService.test.ts`, driver-side regression suite), `VERIFY-156` (`electron/agent/runtime/agent-tool-executor.test.ts`, telemetry branch), `VERIFY-157` (`electron/agent/runtime/chat-agent-runner.telemetry.test.ts`, four `vi.hoisted`-style mock tests), `VERIFY-158` (`electron/services/videoRetrieveService.telemetry.test.ts`, four main-video telemetry tests covering `eventId` reuse for `/video/retrieve`, processing-poll non-terminal contract, `download_url` non-leakage in serialized completion payload, and dispatcher-throw sanitization through `classifyMediaFailure`). `scripts/verify-repo-handoff-hygiene.cjs` loop bound extended 155 → 158 (header comment documents the deliberate governance extension for each new verifier). Final reports: `docs/reports/MEDIA_PREVIEW_TRAFFIC_INSPECTOR_REMEDIATION_REPORT_2026-07-26.md` (Phases A–D), `docs/summary_of_work.md` Session History entries for 2026-07-27 (Phases A–D closure + Phase C emitter close-out). External acceptance for Save As, paid-provider Traffic Inspector export review, and cross-platform packaged progress remains the same `VF-VERIFY-005` evidence row. Validation matrix (current worktree): `npm run lint:eslint` 0 warnings (`--max-warnings=0`), `npm run typecheck` 0 errors (renderer + Electron), `npm test` 4817 passed / 1 skipped / 441 files (379.99 s under Node 22.13.1 / npm 10.9.2), `npm run test:electron` 836 / 836 in 89.73 s (86 files), `npm run verify:safety-guard` PASS, `npm run verify:contracts` 103 PASS, `npm run verify:markdown-links` 245 files OK, `npm run verify:repo-handoff-hygiene` PASS (extended 155 → 158), `npm run verify:i18n` 12 / 12, `npm run verify:i18n-hardcoded-regressions` 0 regressions, `npm run build` PASS. Local working tree currently has 8 modified + 4 added from the Phase A–D + Phase C emitter wiring — none committed, none pushed.

`VF-PR-78-JINA-CRLF-001 — Jina header CRLF/null-byte injection guard (re-applied 2026-07-27).` Originally submitted as PR #78 by Jules and closed in the 52–81 bulk closure. Re-assessed and re-applied: adds `/^[^\r\n\0]*$/.test(value)` validation to the `server.ts` Jina forwarder (`createServerApp()` line 717) so renderer-supplied headers containing carriage returns, line feeds, or NUL bytes are dropped alongside the existing key-based allowlist, plus a `server.test.ts` regression case `drops Jina headers with malicious values (CRLF/Null injection)` that asserts `X-Return-Format: "markdown\r\nInject: true"`, `X-Wait-For-Selector: "body\0"`, and `X-With-Iframe: "true\n"` are all absent from the upstream request. 89 server tests PASS. PR #77 was rejected — its diff also deletes `.github/workflows/ci.yml`, `codeql.yml`, `dependency-review.yml`, and `release.yml`, which would strip every CI protection from main.

`VF-CONTEXT-MENUS-001 — Right-click context menus + Image Studio Save As routing (closed 2026-07-27).` Reported regression: right-click on chat messages, gallery cards, image Studio lightbox, and image Tools result panel was silently swallowed in Electron, and the image Tools "Download" button only invoked a browser anchor in Electron instead of the native Save As dialog. Closed by `src/components/ui/ContextMenu.tsx` (reusable portal-menu primitive with `useContextMenu()` hook + `<ContextMenu>` JSX, positioned via `setProperty` to honor `VERIFY-007` inline-style invariant) plus surface wiring on `src/components/chat/message-bubble.tsx` (user + assistant bubble wrappers, items built from the existing prop callbacks so Render-only / Fork / Delete-from / Regenerate-from / Regenerate / Create Scene / Delete all reuse the canonical message-bubble attribute keys), `src/components/gallery/media-card.tsx` (article-level `onContextMenu` while the thumbnail button keeps its multi-select toggle via `e.stopPropagation()`), `src/components/image/image-view.tsx` (lightbox image now opens Save As → existing `downloadImage()` IPC and Copy → `copyText`). The user-reported Save-As bug is closed by rewriting `src/components/image/image-tools.tsx` `downloadResult()` to route through `desktopMedia.persistGeneratedImage()` plus `desktopFiles.saveGeneratedMedia()` on Electron (native `dialog.showSaveDialog` via `app:media:save-generated` → atomic `saveGeneratedMediaAs`), with an URL.createObjectURL/revokeObjectURL Web fallback. New canonical keys `actions.{saveAs, open, lock, favorite, unfavorite, regenerate, download}` and `media:contextMenu.*` added to en-US + propagated as `__MISSING__:` placeholders to all 11 non-en-US catalogs via `node scripts/sync-catalogs.cjs`; `package.json` `verify:i18n` switches to `--allow-missing-markers` so new keys do not regress the gate before first-pass machine-translation under `VF-I18N-NATIVE-REVIEW-001` runs. Validation: zero-warning ESLint, both TypeScript projects clean, full Vitest 4817 passed / 1 skipped / 442 files, 103-pass contract suite, all `verify:*` segments, Vite+esbuild+Electron build. Headed native-dialog correctness remains external under `VF-VERIFY-005`.

`VF-CUSTOM-PROTOCOL-CORS-001 — Custom-protocol CORS regression guard (closed by `VERIFY-155`).` Electron custom schemes rendered to the renderer (`venice-media://`, `venice-tts://`, `venice-character-cache://`) were registered through `protocol.registerSchemesAsPrivileged` without `corsEnabled: true`, so Chromium rejected every cross-origin request (`<video src="venice-media://…">` from `http://localhost:5173` and from the packaged `file://` renderer) with `net::ERR_FAILED` even with `stream: true` set on the audio/video schemes. Closed by literally setting `corsEnabled: true` on every renderer-consumed scheme in `electron/main.ts` plus a centralised `evaluateCustomProtocolAccess` `electron/utils/customProtocolAccess.ts` helper (the origin guard was previously inline in `characterImageCacheProtocol.ts` and absent on `generatedMediaStore.ts`); regression coverage lives in `electron/utils/customProtocolAccess.test.ts`, `electron/utils/characterImageCacheProtocol.test.ts`, `electron/services/generatedMediaStore.test.ts`, and `scripts/verify-custom-protocol-privileges.{cjs,test.ts}`. AGENTS.md guard row, `verify:repo-handoff-hygiene` loop bound (154→155), `package.json` `verify:custom-protocol-privileges` script, and `verify:contracts:static` chain extend the contract. Headed `npm run dev:electron` validation (renderer `<video readyState>` / `currentTime` advance) and packaged cross-platform Save As remain external acceptance rows under `VF-VERIFY-005`; the static audit plus unit tests are the in-scope evidence.

External signed / paid / two-device / screen-reader release evidence remains the only release blocker:

1. **VF-VERIFY-005 — Produce signed, paid-operation and manual release evidence (P1 release work; externally blocked).** Run signed/notarized macOS and signed Windows clean-install/update, secure-storage, paid generation, restart recovery, two-device sync, and screen-reader/high-zoom/theme/sound QA without recording secrets. Image Inspector acceptance must exercise current live vision metadata with valid PNG, JPEG, and WebP inputs, confirm provider acceptance and structured output, and verify exported Traffic Inspector data contains no raw image payload. Video acceptance includes paid-provider queue/direct-MP4/download-URL completion, restart during queued/generating/retrieving/saving, headed playback/scrubbing, native Save As, and Media Studio reconciliation; see `docs/reports/historical/VIDEO_PIPELINE_AND_RESEARCH_BROWSER_DEACTIVATION_2026-07-18.md`. Detailed broader evidence, root cause, automated validation, manual QA and acceptance criteria remain in `docs/reports/historical/INTENDED_FEATURE_VERIFICATION_2026-07-15.md`. The dependency-migration tree passes hosted Node 22 CI, coverage, CodeQL, macOS/Windows sensitive tests, packaged Electron smoke jobs, the complete correctness surface, aggregate contracts, and unsigned arm64 and x64 macOS DMG/ZIP builds with checksums. Completion requires resources absent from this environment: GitHub has no release-signing secrets, this Mac has no valid code-signing identity, no second device is available, and paid provider operations require explicit expenditure authorization and credentials. These missing prerequisites must not be relabeled as successful QA.

2. **VF-UX-REPRO-001 — Reconcile the screenshot-specific AI Research symptom (P3 evidence-needed).** The 2026-07-18 report referenced an attached photo, but no image was present in the task payload. Section-wide Research progress and Web Scrape provider recovery are covered by `VERIFY-143`; any additional visual/error-state defect must be reproduced from the missing screenshot or equivalent exact steps before implementation and closure.

3. **VF-DOCUMENT-AGENT-001 — Complete release hardening for the Document Agent (P1).** `VERIFY-145..154` now lock the canonical 14-tool registry, immutable managed revisions, exact one-time approvals, hostile path policy, seven validated serializers, bounded no-shell workspace reads/search, staged expected-hash mutations, typed preload IPC, native export, redacted hash-chained audit, the Documents review UI, and attachment-to-managed-document promotion (`VERIFY-154`). Remaining acceptance work is intentionally fail-closed and not exposed as autonomous capability: source-blob retention controls, isolated/fuzzed DOCX/PDF parsing, full workspace changeset/move/recoverable-trash review UI, canonical Chat/Workflow/Project tool-loop integration, operation-journal crash recovery, packaged cross-platform filesystem QA, and the complete manual QA matrix from `docs/audits/Records/Function_calling_todo.md`.

4. **VF-I18N-NATIVE-REVIEW-001 — complete qualified linguistic and packaged localization acceptance (P2 external/reviewer work).** The runtime-localization implementation is locally closed: all 12 bundled catalogs have complete structural/runtime-surface coverage, the expanded production hardcoded-prose scan and exact baseline are zero, Arabic RTL and live locale switching have rendered evidence, and `en-US` is source-complete. The 11 non-English catalogs intentionally remain `first-pass-machine` and `isProductionComplete=false` until a qualified reviewer records per-locale approval and date in `docs/i18n/native-review-status.json`. Remaining acceptance is native terminology/grammar review plus signed packaged Windows/macOS, keyboard-only, screen-reader, high-zoom, and restart-persistence verification under `VF-VERIFY-005`. Historical implementation evidence is indexed in `docs/reports/historical/RUNTIME_I18N_FULL_UI_REMEDIATION_REPORT_2026-07-26.md`; closed implementation rows are not duplicated in this current-work roadmap.

## Active Audit Reconciliation

The most recent local audit lives at `docs/audits/Records/Venice_Forge-audit-evidence-20260717-031029/EVIDENCE_MANIFEST.md`. Its twelve findings (`VF-SCAN-20260717-031029-001..012`) are tracked here; session evidence, regression-guard diffs, and validation results belong in `docs/summary_of_work.md`. Status here reflects only the live tree; audit text is immutable.

- **P1 release blockers — all closed in 2026-07-17 session.**
  - `VF-SCAN-20260717-031029-001` (transcription client): closed — canonical `/audio/transcriptions` client with closed allowlists in `src/services/veniceClient/transcription.ts`; wired through `src/hooks/use-audio.ts`. Regression: `src/services/veniceClient/transcription.test.ts`, `src/hooks/use-audio.test.tsx`.
  - `VF-SCAN-20260717-031029-002` (provider `safe_mode` wiring): closed — runtime snapshot sync at `electron/services/runtimeSafetySettings.ts`; web pre-dispatch at `src/services/veniceClient/fetch.ts`; main-process override at `electron/services/guardPipeline.ts:95-104`; persistence at `electron/services/configService.ts`. Regression: `tests/safety/veniceSafeMode.test.ts`, `tests/safety/guardPipeline.test.ts`.
  - `VF-SCAN-20260717-031029-003` + `VF-SCAN-20260717-031029-005` (profile-isolated chat deletion and TTS cache): closed — `electron/services/chatStorage.ts` adds `purgeProfileChatHistory`; `electron/services/chatTtsBridge.ts` rebuilds the cache around per-profile directories; `electron/ipc/handlers/chatTtsHandlers.ts` and `electron/main.ts` route the `venice-tts://` protocol through the active session. Regression: `electron/services/chatStorage.test.ts`, `electron/services/chatTtsBridge.test.ts`, `electron/services/profilePurge.test.ts`, `src/services/chatTtsController.test.ts`.
- **P2 durability — all closed in 2026-07-17 session.**
  - `VF-SCAN-20260717-031029-004` (bounded test runner): closed — `scripts/run-bounded-test-shards.cjs` now kills the full process tree (POSIX group kill + Windows `taskkill /T /F`).
  - `VF-SCAN-20260717-031029-006` (shutdown log race): closed — `electron/services/appShutdownCoordinator.ts` runs durable cleanup in parallel and sequences `flushLogs()` as the final ordered phase; stderr fallback for late diagnostic emissions.
  - `VF-SCAN-20260717-031029-007` (roadmap overstatement): closed — this file.
- **P3 bounded debt — still open.**
  - `VF-SCAN-20260717-031029-008` (verify-provider-adapters portability): tracked only; `vi.mock('electron')` relocation to `tests/setup.ts` deferred to a separate work order.
  - `VF-SCAN-20260717-031029-009` (`enhancePrompt` extraction): tracked only; `@deprecated` no-op fallthrough at `src/shared/safety/promptPayloadExtractor.ts` deferred to a separate work order.
  - `VF-SCAN-20260717-031029-010..012` (documentation trackers): tracked only via `docs/summary_of_work.md` Open TODO Ledger; no code change required.

## Audit Input

- The 2026-07-17 03:10 deep scan was fully remediated. The current task listing in this roadmap is the only live truth surface for the audit's findings.
- Prior audit evidence bundles (`Venice_Forge-audit-results-20260716-224749`) live under the `docs/audits/Records/` history directory and are no longer authoritative retained evidence.
- `VF-SCAN-20260716-001..024` were reconciled in the 2026-07-16 current-commit session recorded in `docs/summary_of_work.md`.
- `VF-SCAN-20260717-001..012` from the prior local audit were closed in the 2026-07-17 prior session (`docs/summary_of_work.md`), subject to the bounded debt inventory above. Findings `010` and `011` remain accepted incremental architecture guidance rather than runtime defects.
- `VF-SCAN-20260717-013..016` remain product/release scope rather than hidden local defects. Deferred providers/transports stay fail-closed, orphan deletion stays analysis-only pending quarantine/rollback, and signed/paid/two-device/headed proof remains represented only by `VF-VERIFY-005`.
- `VF-AUDIT-001` was completed through the live GitHub APIs on 2026-07-16: `main` is protected with strict required checks, review/code-owner/last-push approval, admin enforcement and conversation resolution.
- `VF-AUDIT-006` was completed in bounded runtime/toolchain batches (Express 5, dotenv 17, proxy middleware 4, PDF.js 6, Electron 43, ESLint 10, React Hooks ESLint 7, globals 17, Vite 8 and the React Vite plugin 6). `@types/node` intentionally stays on 22 to match the runtime and TypeScript stays on 5.8 because `typescript-eslint` 8.64 declares `<6.1.0`. `npm outdated` showing Node 26 types or TypeScript 7 does not constitute an applicable update for this project.
