--- src/shared/safety/childExploitationGuard.test.ts
+++ src/shared/safety/childExploitationGuard.test.ts
@@ -149,11 +149,11 @@

-// ---------------------------------------------------------------------------
-// BUG-001 regression: leet-fold must not destroy digits for age extraction
-// ---------------------------------------------------------------------------
-
-describe("age extraction regression (leet-fold bug #1)", () => {
+// ---------------------------------------------------------------------------
+// Age extraction: leet-fold must not destroy digits
+// ---------------------------------------------------------------------------
+
+describe("age extraction (leet-fold digit preservation)", () => {
   it("blocks '13 years old' with sexualization signal", () => {
-    // Before fix: "13 years old" → leet-folded → "ie years old" → age not found
+    // Ensures "13 years old" is not leet-folded into "ie years old" before age extraction
     const d = block("nude 13 years old girl");
     expect(d.allow).toBe(false);
